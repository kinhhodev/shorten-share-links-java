# Leave empty when:
# - Vite dev proxy (vite.config.ts) → /api on :5173, or
# - Docker production image → nginx proxies /api to the API (same origin on port 80).
# Otherwise set the full API base URL (no trailing slash), e.g.:
# VITE_API_BASE_URL=http://localhost:8080
# VITE_TURNSTILE_SITE_KEY=
