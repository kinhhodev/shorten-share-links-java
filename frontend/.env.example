# Leave empty when using Vite dev proxy (see vite.config.ts) so requests go to /api on :5173.
# For production or when the API is on another origin, set the full backend base URL (no trailing slash).
# VITE_API_BASE_URL=http://localhost:8080
